name: "Autograding"
description: "Autograding for GitHub Classroom"
author: "@cisc2200"
inputs:
  token:
    default: ${{ github.token }}
outputs:
  points:
    description: 'Final points'

runs:
  using: 'docker'
  image: 'Dockerfile'
